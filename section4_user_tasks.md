# Розділ 4 БКР: завдання, які потрібно виконати вручну

Цей файл містить покрокові інструкції для виконання тих частин
експериментальної програми Розділу 4, які потребують ручної взаємодії з
браузером (запуск симулятора, виміри `fpsEma`/`tickTimeMs`/heap,
скріншоти UI, soak-тести). Автоматизована частина експерименту вже
виконана харнесом `src/__bench__/chapter4.test.ts` і збережена у
`section4_data.json`.

Кожне завдання має формат:

- **Що зробити**: дія, яку треба виконати.
- **Як зробити**: покрокова інструкція.
- **Що зафіксувати**: які саме числа/скріншоти/файли треба зберегти.
- **Куди вставити**: до якого підрозділу/таблиці/місця у `БКР_Тесля.tex`.
- **Очікуваний результат**: орієнтовний діапазон значень для самоперевірки.

Усі плейсхолдери у тексті позначені у форматі `[ПЛЕЙСХОЛДЕР: Бx]`, де
`Бx` — номер пункту з цього файлу.

---

## Передумови (виконати один раз)

1. Встановити Node.js ≥ 18 з `nodejs.org`.
2. У корені проєкту виконати:
   ```powershell
   npm install
   npm run build
   npm start
   ```
   Симулятор відкриється за адресою `http://localhost:3000`.
3. Підготувати три браузери: Chrome 120+, Edge 120+, Firefox 121+
   (мінімальні вимоги з підрозд. 4.2 БКР).
4. У Chrome/Edge відкрити DevTools (F12), вкладку **Performance**, у
   налаштуваннях задати CPU throttling = **No throttling**.

---

## Б1. Виміри UI-продуктивності у трьох браузерах (Табл. 4.7, NF3, NF4)

### Що зробити
Для кожного з трьох браузерів зафіксувати `fpsEma`, `tickTimeMs`,
розмір heap і час старту симуляції на пресеті `sin1`.

### Як зробити
1. Відкрити симулятор у браузері.
2. Натиснути **Очистити**, потім обрати пресет `sin1` у селекторі.
3. Натиснути **Пуск**.
4. Зачекати ≈ 5 секунд, доки EMA-метрики стабілізуються.
5. На панелі **Metrics** прочитати:
   - `fpsEma` (кадрів/с)
   - `tickTimeMs` (мс)
6. У DevTools → **Memory** → **Take heap snapshot** → зафіксувати **Total Size**.
7. Відкрити DevTools → **Console**. Натиснути **Пуск** і зафіксувати
   два рядки, які автоматично з'являться:
   - `[NF2] Час відгуку UI (перший тік rAF): X мс` — **це і є «Старт»** для
     Табл. 4.7 / NF2 (вимірює затримку браузерного event-loop + rAF).
   - `[КРИТИЧНИЙ ШЛЯХ] Час до першого результату: Y мс` — інформаційне
     значення; для DFT4x4 очікується ≈ 3400 мс (це властивість графа
     обчислювача, а не UI-продуктивність).

> **Примітка щодо критичного шляху.**  
> Граф DFT4x4 має 4 яруси з параметрами `edge.delay = 600 мс`,
> `latency_DFT4 = 400 мс`, `latency_twiddle = 200 мс`. Критичний шлях:
> 600 + 400 + 600 + 200 + 600 + 400 + 600 = **3400 мс simTime**.
> При `speed = 1` це дорівнює реальному часу очікування першого результату.
> Це **не порушення NF2** — NF2 регламентує відгук UI (кнопки/повзунок),
> а не час обчислення.

### Що зафіксувати
| Браузер | fpsEma | tickTimeMs | Heap (МБ) | Старт/NF2 (мс) | Маркер |
|---------|--------|------------|-----------|----------------|--------|
| Chrome 120 |   |   |   |   | [П1.1]–[П1.4] |
| Edge 120 |   |   |   |   | [П1.5]–[П1.8] |
| Firefox 121 |   |   |   |   | [П1.9]–[П1.12] |

> Колонка «Старт/NF2» = значення з рядка `[NF2]` у консолі DevTools.

### Куди вставити
- `БКР_Тесля.tex`, Табл. 4.7 (підрозд. 4.3.5). Замінити плейсхолдери
  `[П1.1]` … `[П1.12]` фактичними числами.
- Табл. 4.10 (підрозд. 4.4.5), рядки NF3 і NF4 — замінити
  `[ПЛЕЙСХОЛДЕР: Б1]` на «виконано» з посиланням на табл. 4.7.

### Очікуваний результат
- `fpsEma` = 60 у всіх трьох браузерах (±2).
- `tickTimeMs` ∈ [1.5, 3.5] мс.
- Heap ∈ [40, 65] МБ.
- **Старт/NF2 ∈ [10, 50] мс** (час відгуку UI, рядок `[NF2]` у консолі).
- Час першого результату (рядок `[КРИТИЧНИЙ ШЛЯХ]`) ≈ 3400 мс —
  відповідає критичному шляху DFT4x4, не є показником UI-відгуку.

Якщо `fpsEma < 50` або `tickTimeMs > 8 мс` — це не норма; перевірити
наявність відкритих DevTools (вони уповільнюють виконання), повторити
вимір з закритими DevTools.

---

## Б2. Перевірка часу відгуку UI (NF2)

### Що зробити
Перевірити, що реакція UI на натискання **Пуск**, **Пауза**, **Крок**,
**Скидання** і на зміну повзунка швидкості не перевищує 100 мс.

### Як зробити
1. У Chrome DevTools → **Performance** → **Record**.
2. Натиснути послідовно: **Пуск**, **Пауза**, **Крок**, **Скидання**,
   потягнути повзунок швидкості з 1× до 5×.
3. Зупинити запис.
4. Для кожної дії знайти найближчу подію `click` (або `input` для повзунка)
   та виміряти час до першого `Paint`/`Frame` після цього.

### Що зафіксувати
Максимальний час відгуку по всіх п'яти діях (одне число), а також
скріншот вкладки Performance з виділеним інтервалом найдовшої події.

### Куди вставити
- `БКР_Тесля.tex`, Табл. 4.10 (підрозд. 4.4.5), рядок NF2 — замінити
  `[ПЛЕЙСХОЛДЕР: Б2]` на «виконано (макс. час відгуку: X мс)».

### Очікуваний результат
- Максимум ≤ 100 мс; типове значення 16–40 мс на десктопі.

---

## Б3. Перевірка вимоги NF8 «зручність» (≤ 2 кліки до основних операцій)

### Що зробити
Підтвердити, що для типового користувача (студента) кожна з основних
операцій з підрозд. 2.6 досягається не глибше двох кліків від головного
екрана.

### Як зробити
Перевірити, що з відкритого головного екрана:
1. **Вибір пресету** доступний у 1 клік (селектор у Controls);
2. **Пуск симуляції** — 1 клік (кнопка Пуск);
3. **Перегляд спектра** — 0 кліків (вже видно у SpectrumView);
4. **Перегляд heatmap активності** — 0 кліків (на полотні);
5. **Зміна швидкості** — 1 клік (повзунок);
6. **Крок симуляції** — 1 клік (кнопка Крок);
7. **Експорт журналу подій** — 2 кліки (кнопка → Save).

### Що зафіксувати
Стислий висновок («виконано: усі основні операції — не глибше 2 кліків»)
з посиланням на скріншот (Б4).

### Куди вставити
- `БКР_Тесля.tex`, Табл. 4.10 (підрозд. 4.4.5), рядок NF8 — замінити
  `[ПЛЕЙСХОЛДЕР: Б3]` на «виконано».

### Очікуваний результат
- Усі сім операцій досягаються ≤ 2 кліки.

---

## Б4. Скріншот робочого екрана симулятора (Рис. 4.2)

### Що зробити
Зробити скріншот UI симулятора у момент активної роботи, на якому одночасно
видно: топологію DFG (GraphView), токени, що рухаються по дугах, панель
керування (Controls), спектральний результат (SpectrumView) і метрики
(Metrics).

### Як зробити
1. Запустити симулятор у Chrome у режимі вікна 1600×1000 (або 1920×1080).
2. Обрати пресет `sin3`, виставити `speed = 0.5`, натиснути **Пуск**.
3. Дочекатись, коли активуються вузли ярусу 1 і з'являться токени на
   дугах ярусу 2.
4. Натиснути **Пауза**.
5. Зробити скріншот (Win+Shift+S або PrtSc).
6. Зберегти як `D:\diploma\changes\media\media\image_fig42.png` (або у
   ту саму папку, що й уже існуючі image1.png тощо).

### Що зафіксувати
- Файл скріншота за вказаним шляхом.

### Куди вставити
- `БКР_Тесля.tex`, підрозд. 4.5 (відразу після рядка «Рис. 4.2.
  Загальний вигляд iнтерфейсу веб-симулятора пiд час роботи. Джерело:
  розроблено автором.»). Додати безпосередньо перед цим рядком:
  `\includegraphics[width=6.5in]{media/media/image_fig42.png}`
- Видалити маркер `[ПЛЕЙСХОЛДЕР: скріншот UI у середині циклу, ...]`.

### Очікуваний результат
- На скріншоті видно щонайменше 4 активні `dft4`-вузли (підсвічені на
  heatmap) і токени на 8–16 дугах.

---

## Б5. Soak-тест (стабільність тривалої роботи)

### Що зробити
Виконати 100 послідовних повних симуляцій підряд і переконатися у
відсутності витоків пам'яті та зависань UI.

### Як зробити
1. У Chrome відкрити DevTools → **Memory** → зробити перший snapshot.
2. Виконати у DevTools Console:
   ```javascript
   (async () => {
     const store = window.__simStore; // якщо експортовано
     for (let i = 0; i < 100; i++) {
       store.applyPreset(i % 2 === 0 ? 'ramp' : 'sin3');
       store.start();
       await new Promise(r => setTimeout(r, 4000));
       store.stop();
     }
   })();
   ```
   (Якщо `window.__simStore` не експортовано — повторити дії руками:
   натиснути **Пуск**, дочекатись завершення (≈ 3.4 с), натиснути
   **Очистити**, зробити це 100 разів. Можна автоматизувати через
   AutoHotkey/PowerToys Mouse Without Borders.)
3. Зробити другий snapshot.
4. Порівняти `Total Size` обох snapshot-ів.

### Що зафіксувати
- Heap до соак-тесту (МБ).
- Heap після соак-тесту (МБ).
- Чи був хоча б один frame > 16 мс під час прогону.

### Куди вставити
- `БКР_Тесля.tex`, підрозд. 4.5, у місці `[ПЛЕЙСХОЛДЕР: підсумки
  soak-тесту, ...]`. Сформулювати як:
  «Soak-тест зі 100 повних симуляцій (5 хв) показав heap (МБ) до/після
  тесту X / Y, зростання — Z%; зависань UI не виникало».

### Очікуваний результат
- Heap до/після відрізняється на ≤ 5 МБ.
- Зависань або frame > 50 мс не повинно бути.

---

## Б6. Стрес-тест із рендерингом кількох копій графа (моніторинг, підрозд. 4.6)

### Що зробити
Перевірити поведінку при одночасному рендерингу кількох копій DFG
(≈ 200–280 вузлів) для оцінки практичної межі React Flow.

### Як зробити
1. Відкрити симулятор.
2. У DevTools Console виконати:
   ```javascript
   const root = document.querySelector('main');
   for (let i = 0; i < 4; i++) {
     const clone = root.cloneNode(true);
     root.parentElement.appendChild(clone);
   }
   ```
3. Натиснути **Пуск** на всіх копіях (можна клікнути по кнопці у кожній
   копії).
4. Прочитати `fpsEma` на одній з копій після стабілізації.

### Що зафіксувати
- `fpsEma` після додавання 4 копій (всього 5 графів × 56 ≈ 280 вузлів).
- Якщо браузер починає лагати — записати спостереження.

### Куди вставити
- `БКР_Тесля.tex`, підрозд. 4.6, у місці `[ПЛЕЙСХОЛДЕР: фактичні
  значення -- див. section4_user_tasks.md п. Б6]`. Сформулювати як:
  «За стрес-тесту з 5 копіями графа (≈ 280 вузлів) fpsEma знижується до X
  кадрів/с, що визначає практичну межу — близько 200 вузлів без
  віртуалізації.»

### Очікуваний результат
- `fpsEma` ∈ [30, 50] кадрів/с при 5 копіях.

---

## Б7. Заповнити календарний план (титул)

### Що зробити
У титульному блоці БКР (рядки 287–294 файла `БКР_Тесля.tex`) заповнити
табличку календарного плану, рядок «!!! ЗАПОВНИТИ (від 15.02/20.04 до
25.05)» зокрема.

### Як зробити
Вписати у графу «Назва етапів роботи» етапи 2–7 у вільному форматі
відповідно до фактичного календарного плану захисту.

### Що зафіксувати
- Кінцеві назви етапів і дати.

### Куди вставити
- `БКР_Тесля.tex`, рядки 287–294 (рядки 2–7 у таблиці «КАЛЕНДАРНИЙ
  ПЛАН»).

### Очікуваний результат
- Усі сім рядків таблиці заповнені; жодного маркера «!!!» у файлі.

---

## Б8. Заповнити дату видачі завдання

### Що зробити
Замінити плейсхолдери `XX.XX.XXXX` і `XX XXXXXXXXX XXXX` у блоці
«ЗАВДАННЯ» (рядки 158–161).

### Як зробити
1. Знайти рядок «Тема роботи: ... затверджена наказом по університету
   від XX.XX.XXXXр. №XXXX-X-XX».
2. Замінити на фактичні дані наказу деканату.
3. Знайти «Термін подання закінченої роботи XX XXXXXXXXX XXXX року».
4. Замінити на фактичну дату.

### Куди вставити
- `БКР_Тесля.tex`, рядки 158–161.

### Очікуваний результат
- Жодного плейсхолдера у титульних блоках.

---

## Запитання, які потребують рішення автора

1. **Бібліографія.** У файлі є *дві* секції бібліографії (рядки
   4234–4286 у форматі `\begin{enumerate}` і 4288–4416 у форматі
   ручного оформлення ДСТУ). Друга секція містить також помилкове
   повторення джерела з №6 («Ecma International»), а також №26 пропущено.
   Потрібно вирішити: (а) залишити лише одну — другу, виправити дублікат
   №6, додати №26; (б) залишити лише першу і додати решту джерел у тому
   ж форматі. Я не вношу зміни без вашого підтвердження, бо це
   стилістичне рішення (BibTeX vs ручне оформлення ДСТУ).
2. **Кількість джерел в анотації.** Анотація стверджує «25 джерел», але
   у списку 27 елементів (з урахуванням дублікату — 26 унікальних).
   Потрібно узгодити: або скоротити список до 25, або виправити
   анотацію.
3. **Рисунок 4.2.** Зараз це плейсхолдер. Потрібен реальний скріншот
   (див. Б4).
4. **Таблиця 4.7.** Усі 12 значень — плейсхолдери (Б1).
5. **Таблиця 4.10.** Чотири рядки (NF2, NF3, NF4, NF8) мають
   плейсхолдери до завершення Б1, Б2, Б3.
6. **Календарний план і дата завдання.** Б7, Б8.
