@startuml
title Рис. 2.5 — Макет інтерфейсу веб-симулятора потокового 16-точкового ШПФ

skinparam shadowing false
skinparam backgroundColor #FFFFFF
skinparam defaultTextAlignment center
skinparam ArrowColor #64748B
skinparam rectangle {
  RoundCorner 12
  BorderColor #334155
  FontColor #0F172A
}

rectangle "ВЕРХНЯ ПАНЕЛЬ — Controls / Toolbar\n(Controls.tsx, Toolbar.tsx)\n────────────\nRun · Pause · Step · Reset │ повзунок швидкості 0.1×…10×\nселектор пресету (sin1 / impulse / ramp / noise) │ редактор latency, delay" as Top #DBEAFE

rectangle "ЦЕНТРАЛЬНА ЗОНА — GraphView\n(GraphView.tsx, React Flow)\n────────────\nПолотно DFG: 56 вузлів, 64 дуги\n● анімація токенів на дугах [t, t+delay]\n● теплова карта активності вузлів (α = 0.92)\n● busy-кільце latency │ зум, перетягування вузлів" as Graph #DCFCE7

rectangle "ПРАВА ПАНЕЛЬ — SpectrumView + Metrics\n(SpectrumView.tsx, Metrics.tsx)\n────────────\nГістограма |X(k)| (лінійний / log)\nthroughput · latency · queue size" as Right #FEF3C7

rectangle "НИЖНЯ ПАНЕЛЬ — TerminalOutput\n(TerminalOutput.tsx, EventLog.tsx)\n────────────\nЖурнал подій onToken / onFire / onOutput\nфільтрація за типом · експорт у файл" as Bottom #EDE9FE

Top -down-> Graph
Graph -right-> Right
Right -down-> Bottom
Graph -down-> Bottom

note right of Right
  Інформаційна ієрархія: найбільша
  площа — графу потоку даних,
  допоміжні панелі по периметру.
  Базові операції — у межах 2 кліків (NF8).
end note
@enduml
